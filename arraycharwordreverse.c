#include<stdio.h>
#include<stdlib.h>

int reverseTab(int tab[],int size);


int main()
{
int n;
int tab[]={'G','R','E','E','N'};
printf(reverseTab);  
    return 0;
   
}

int reverseTab(int tab[],int size){
    int n;
    int size = sizeof(tab) / sizeof(tab[0]);
    for (int i=n;i>=0;i--){
        printf("%d",tab[i]);
    }
}
